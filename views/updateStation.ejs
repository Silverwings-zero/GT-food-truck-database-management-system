<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Station</title>
</head>
<body>
    <form action="/updateStation" method="POST">
        <h1>Update Station</h1>
        <div>
            <p id="sname">Name: </p>
        </div>
        <div>
            <label for="capacity">Capacity: </label>
            <input type="number" name="capacity" id="capacity" required>
        </div>
        <div>
            <label for="sb">Sponsored Building: </label>
            <select name="sb" id="sb" required>
                <%  var buildingName = JSON.parse(buildingName);
                    for (let i = 0; i < buildingName.length; i++) { %>
                    <option value="<%= buildingName[i] %>"><%= buildingName[i] %></option> 
                <% } %>
            </select>
        </div>
        <button>Back</button>
        <script>
            var bu_name = <%- b_name %>;
            var st_name = <%- s_name %>;
            var capacity =  <%- cap %>;
            document.getElementById('sname').innerHTML = `Name: ${st_name}`;
            document.getElementById('capacity').value = capacity;
            document.getElementById('sb').value = bu_name;
        </script>
        <input type="hidden" name="s_name" id="s_name" value="<%= s_name %>">
        <button type="submit">Create</button>
    </form>
</body>
</html>